-- ============================================================
-- V60：多向量架構下校準 skill.rag.min_score 0.35 → 0.45（v1.6.2）
--
-- 依據：tasks-v1.6.2.md Phase 7 校準實驗（真 embedding + 真 pgvector）：
--   正例（單詞 / 語句 / 部分匹配對相關 Skill 的 MAX 分）0.58 ~ 0.89，
--   雜訊（跨 skill 無關 + 負例查詢）0.10 ~ 0.33。
--   0.45 全收正例、全擋雜訊，兩側安全邊際 > 0.12。
--
-- 僅在仍為 v1.6.0/V58 出廠值 '0.35' 時更新（保留人工調整值）。
-- 獨立成 V60 而非併入 V59 尾端：V59 已於環境套用，migration 不可變。
-- ============================================================

UPDATE system_setting
SET value = '0.45'
WHERE key = 'skill.rag.min_score'
  AND value = '0.35';
